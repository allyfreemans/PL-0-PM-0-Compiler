var x, w;
const y = 5;
begin
  read x;
  if x < y then
    begin
	x := 7;
	x := x + 1 + (2 * 2);
	x := x + 1 + (2 * 2);
	x := x + 1 + (2 * 2);
	x := x + 1 + (2 * 2);
	x := x + 1 + (2 * 2);
	end;
  write x;
  write w;
end.